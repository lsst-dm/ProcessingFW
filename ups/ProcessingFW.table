envPrepend(PATH, ${PRODUCT_DIR}/bin)
envPrepend(PATH, ${PRODUCT_DIR}/tools)
envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
envSet(CPN_LOCK_BASE,/grid/data)
setupRequired(python)
#setupRequired(coreutils)
